<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

	<targets>
		<target name="logconsole" xsi:type="ColoredConsole" layout="${date:format=HH\:mm\:ss} [${level:format=FirstCharacter}] [${logger}] ${message}" >
			<highlight-row condition="level == LogLevel.Trace" foregroundColor="Gray" />
			<highlight-row condition="level == LogLevel.Debug" foregroundColor="White" />
			<highlight-row condition="level == LogLevel.Info" foregroundColor="Green" />
			<highlight-row condition="level == LogLevel.Warn" foregroundColor="Yellow" />
			<highlight-row condition="level == LogLevel.Error" foregroundColor="Red" />
			<highlight-row condition="level == LogLevel.Fatal" foregroundColor="Red" backgroundColor="White" />
		</target>

		<target name="logfile" xsi:type="File"
				fileName="${basedir}/log/log.txt"
				archiveFileName="${basedir}/log/archive/log.{####}.txt"
				archiveNumbering="DateAndSequence"
				archiveEvery="FileSize=10000000"
				maxArchiveDays ="3"
				compressArchiveFiles="true"
				archiveOldFileOnStartup="true"
				createDirs="true"
				layout="${date:s} [${level:format=FirstCharacter}] [${logger}] ${message}"
				/>
	</targets>

	<rules>
		<logger name="*" minlevel="Info" writeTo="logconsole" />
		<logger name="*" minlevel="Trace" writeTo="logfile" />
	</rules>
</nlog>